Write two recursive functions: one that finds the biggest element of a list and another that finds the smallest. You should use the like this:

```
MyList.max([4, 2, 16, 9, 10])
# => 16
MyList.min([4, 2, 16, 9, 10])
# => 2
```
